.reduce()
.filter()
.map((안에 든 것들 각각, i번째 인덱스, 메소드가 호출된 배열 자체)=>{})
.join("") - 배열들을 문자열로 다닥다닥 붙임

true와 false는 각각 1과 0의 숫자를 가짐
+를 붙여주면 불리언 값이 숫자값으로 변환됨

조건연산자에서도 사용가능
k%2?k+1:k-1;
k%2가 1, 즉 홀수, 즉 true
k%2가 0, 즉 짝수, 즉 false

.replaceAll(바꿀 대상, 바뀔 모습) => 이걸로 특정 부분만 대문자로 바꿔준다거나 할 수 있음

.slice(시작, 끝)
       ㄴ 시작 부분만 쓰면 끝까지 알아서 자름